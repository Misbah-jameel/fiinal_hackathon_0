# Platinum Tier Requirements
# Additional dependencies for Cloud/Local deployment

# Core dependencies (inherited from main requirements.txt)
# python-dotenv
# playwright
# google-api-python-client
# tweepy
# etc.

# Cloud deployment
docker>=6.0.0
kubernetes>=28.0.0

# Monitoring and health
prometheus-client>=0.19.0
health-check>=1.3.0

# Encryption (for vault sync)
cryptography>=41.0.0

# Desktop notifications (Local Agent)
# Windows: win10toast (installed separately)
# macOS: Built-in (osascript)
# Linux: libnotify (system package)

# Additional utilities
schedule>=1.2.0
apscheduler>=3.10.0

# Git operations
GitPython>=3.1.40

# HTTP client for APIs
httpx>=0.25.0
aiohttp>=3.9.0

# Data validation
pydantic>=2.5.0

# Logging and monitoring
structlog>=23.2.0
python-json-logger>=2.0.0

# Process management
psutil>=5.9.0

# Testing (development)
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
